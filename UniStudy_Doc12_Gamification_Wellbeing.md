**UniStudy AI**  
Document 12 of 18  
**Gamification, Wellbeing & Personalisation**  
XP & Levels · Badges · Study Personality Quiz · Tutor Avatar · Burnout Monitor · Effort Tracker · Sleep Tracker · Anxiety Check-in · Break Suggester · Motivational Quotes  
Prerequisite: Docs 1–11 complete — profiles table with XP/streak/level columns

**Table of Contents**
=====================

**1\. DOCUMENT OVERVIEW**  
This is Document 12 of 18. It builds the engagement and wellbeing layer — the features that keep students motivated over a full semester. The XP and badge system rewards meaningful actions (not just time spent). The wellbeing features are designed to be lightweight and non-intrusive — a single emoji tap, never a long form. All personalisation preferences are stored in the profiles table established in Doc 1.

<table><tbody><tr><td>WELLBEING PHILOSOPHY: No feature in this document should feel like surveillance or pressure. All mood and sleep tracking is opt-in, private, and never shown to anyone else. The goal is to give students useful insights about their own study patterns, not to judge or monitor them.</td></tr></tbody></table>

**2\. XP SYSTEM & LEVEL PROGRESSION**

**2.1 XP Awards by Action**
---------------------------

<table><tbody><tr><td><strong>Action</strong></td><td><strong>XP Awarded</strong></td><td><strong>Trigger Location</strong></td></tr><tr><td>Quiz question correct</td><td>10 XP per correct answer</td><td>/api/quiz/attempt</td></tr><tr><td>Flashcard reviewed (good/easy)</td><td>5 XP per card</td><td>/api/flashcards/review</td></tr><tr><td>Lecture completed (all slides viewed)</td><td>50 XP</td><td>/api/position (when slide = last)</td></tr><tr><td>Past paper attempted</td><td>100 XP</td><td>/api/past-papers (on attempt creation)</td></tr><tr><td>Past paper completed</td><td>150 XP</td><td>/api/past-papers/grade</td></tr><tr><td>Assignment submitted</td><td>30 XP</td><td>/api/assignments (PATCH to submitted)</td></tr><tr><td>Flashcard shared to community</td><td>50 XP</td><td>/api/past-papers/community (POST)</td></tr><tr><td>Study goal completed</td><td>75 XP per goal</td><td>/api/goals cron check</td></tr><tr><td>Battle won</td><td>100 XP</td><td>FlashcardBattle component</td></tr><tr><td>Battle lost</td><td>50 XP</td><td>FlashcardBattle component</td></tr><tr><td>7-day streak maintained</td><td>200 XP</td><td>Streak cron (Doc 10)</td></tr><tr><td>30-day streak maintained</td><td>500 XP</td><td>Streak cron (Doc 10)</td></tr></tbody></table>

**2.2 Level Thresholds**
------------------------

<table><tbody><tr><td><strong>Level</strong></td><td><strong>Title</strong></td><td><strong>XP Required</strong></td><td><strong>Badge Colour</strong></td></tr><tr><td>1</td><td>Freshman</td><td>0</td><td>Gray</td></tr><tr><td>2</td><td>Sophomore</td><td>500</td><td>Bronze</td></tr><tr><td>3</td><td>Junior</td><td>1,500</td><td>Bronze</td></tr><tr><td>4</td><td>Senior</td><td>3,000</td><td>Silver</td></tr><tr><td>5</td><td>Graduate</td><td>6,000</td><td>Silver</td></tr><tr><td>6</td><td>Scholar</td><td>10,000</td><td>Gold</td></tr><tr><td>7</td><td>Distinction</td><td>18,000</td><td>Gold</td></tr><tr><td>8</td><td>Honours</td><td>30,000</td><td>Platinum</td></tr><tr><td>9</td><td>Summa</td><td>50,000</td><td>Diamond</td></tr><tr><td>10</td><td>Legend</td><td>100,000</td><td>Rainbow</td></tr></tbody></table>

**2.3 Level Calculator — /lib/xp.ts**
-------------------------------------

<table><tbody><tr><td>// src/lib/xp.ts<br>const LEVELS = [<br>{ level:1, title:'Freshman', xpRequired:0, badge:'gray' },<br>{ level:2, title:'Sophomore', xpRequired:500, badge:'bronze' },<br>{ level:3, title:'Junior', xpRequired:1500, badge:'bronze' },<br>{ level:4, title:'Senior', xpRequired:3000, badge:'silver' },<br>{ level:5, title:'Graduate', xpRequired:6000, badge:'silver' },<br>{ level:6, title:'Scholar', xpRequired:10000, badge:'gold' },<br>{ level:7, title:'Distinction',xpRequired:18000, badge:'gold' },<br>{ level:8, title:'Honours', xpRequired:30000, badge:'platinum' },<br>{ level:9, title:'Summa', xpRequired:50000, badge:'diamond' },<br>{ level:10,title:'Legend', xpRequired:100000, badge:'rainbow' },<br>];<br>export function getLevel(xp: number) {<br>let current = LEVELS[0];<br>for (const l of LEVELS) { if (xp &gt;= l.xpRequired) current = l; else break; }<br>const nextLevel = LEVELS[current.level] || null;<br>const xpToNext = nextLevel ? nextLevel.xpRequired - xp : 0;<br>const progress = nextLevel ? ((xp - current.xpRequired) / (nextLevel.xpRequired - current.xpRequired)) * 100 : 100;<br>return { ...current, xpToNext, progress: Math.min(100, Math.round(progress)) };<br>}<br>// Update level in DB after XP change<br>export async function syncLevel(userId: string, xp: number, supabase: any) {<br>const { title } = getLevel(xp);<br>await supabase.from('profiles').update({ level: title }).eq('id', userId);<br>}</td></tr></tbody></table>

**2.4 XPBar.tsx Component**
---------------------------

<table><tbody><tr><td>// src/components/study/XPBar.tsx<br>'use client';<br>import { motion } from 'framer-motion';<br>import { getLevel } from '@/lib/xp';<br>import { Sparkles } from 'lucide-react';<br>export function XPBar({ xp }: { xp: number }) {<br>const lvl = getLevel(xp);<br>return (<br>&lt;div className='flex flex-col gap-1'&gt;<br>&lt;div className='flex items-center justify-between'&gt;<br>&lt;div className='flex items-center gap-2'&gt;<br>&lt;Sparkles className='h-4 w-4 text-[var(--color-amber-500)]'/&gt;<br>&lt;span className='text-sm font-bold text-[var(--text-primary)]'&gt;{lvl.title}&lt;/span&gt;<br>&lt;span className='text-xs text-[var(--text-muted)]'&gt;Lv {lvl.level}&lt;/span&gt;<br>&lt;/div&gt;<br>&lt;span className='text-xs font-mono text-[var(--text-muted)]'&gt;{xp.toLocaleString()} XP&lt;/span&gt;<br>&lt;/div&gt;<br>&lt;div className='w-full bg-[var(--bg-subtle)] rounded-full h-2 overflow-hidden'&gt;<br>&lt;motion.div<br>className='h-2 rounded-full bg-gradient-to-r from-[#7C3AED] to-[#F59E0B]'<br>initial={{width:0}} animate={{width:lvl.progress+'%'}} transition={{duration:1,ease:[0,0,0.2,1]}}<br>/&gt;<br>&lt;/div&gt;<br>{lvl.xpToNext &gt; 0 &amp;&amp; (<br>&lt;p className='text-xs text-[var(--text-muted)]'&gt;{lvl.xpToNext.toLocaleString()} XP to {LEVELS_MAP[lvl.level+1]||'max'}&lt;/p&gt;<br>)}<br>&lt;/div&gt;<br>);<br>}<br>const LEVELS_MAP: Record&lt;number,string&gt; = {<br>2:'Sophomore',3:'Junior',4:'Senior',5:'Graduate',<br>6:'Scholar',7:'Distinction',8:'Honours',9:'Summa',10:'Legend',<br>};</td></tr></tbody></table>

**3\. BADGE SYSTEM**

**3.1 Badge Definitions — /lib/badges.ts**
------------------------------------------

<table><tbody><tr><td>// src/lib/badges.ts<br>export interface Badge {<br>id: string;<br>name: string;<br>emoji: string;<br>description: string;<br>condition: (stats: UserStats) =&gt; boolean;<br>}<br>interface UserStats {<br>xp: number;<br>streak: number;<br>quizCount: number;<br>flashcardCount:number;<br>paperCount: number;<br>lectureCount: number;<br>perfectQuiz: boolean; // 100% on any quiz<br>nightStudy: boolean; // studied after 11pm<br>}<br>export const BADGES: Badge[] = [<br>// Streak badges<br>{ id:'streak_3', name:'On a Roll', emoji:'🔥', description:'3-day study streak', condition:s=&gt;s.streak&gt;=3 },<br>{ id:'streak_7', name:'Week Warrior', emoji:'⚡', description:'7-day study streak', condition:s=&gt;s.streak&gt;=7 },<br>{ id:'streak_30', name:'Iron Scholar', emoji:'🗿', description:'30-day study streak', condition:s=&gt;s.streak&gt;=30 },<br>// XP badges<br>{ id:'xp_1000', name:'Rising Star', emoji:'⭐', description:'Earned 1,000 XP', condition:s=&gt;s.xp&gt;=1000 },<br>{ id:'xp_10000', name:'Knowledge Seeker',emoji:'🎓',description:'Earned 10,000 XP', condition:s=&gt;s.xp&gt;=10000 },<br>{ id:'xp_100000', name:'Legend', emoji:'👑', description:'Earned 100,000 XP', condition:s=&gt;s.xp&gt;=100000 },<br>// Quiz badges<br>{ id:'quiz_10', name:'Quiz Rookie', emoji:'📝', description:'Completed 10 quizzes', condition:s=&gt;s.quizCount&gt;=10 },<br>{ id:'quiz_100', name:'Quiz Champion', emoji:'🏆', description:'Completed 100 quizzes', condition:s=&gt;s.quizCount&gt;=100 },<br>{ id:'perfect', name:'Perfectionist', emoji:'💯', description:'100% on a quiz', condition:s=&gt;s.perfectQuiz },<br>// Flashcard badges<br>{ id:'fc_100', name:'Card Shark', emoji:'🃏', description:'Reviewed 100 flashcards',condition:s=&gt;s.flashcardCount&gt;=100 },<br>{ id:'fc_1000', name:'Memory Master', emoji:'🧠', description:'Reviewed 1,000 flashcards',condition:s=&gt;s.flashcardCount&gt;=1000 },<br>// Paper badges<br>{ id:'paper_5', name:'Exam Veteran', emoji:'📋', description:'Completed 5 past papers',condition:s=&gt;s.paperCount&gt;=5 },<br>// Special badges<br>{ id:'night_owl', name:'Night Owl', emoji:'🦉', description:'Studied after 11pm', condition:s=&gt;s.nightStudy },<br>{ id:'all_courses',name:'Polymath', emoji:'🌟', description:'Studied 5+ courses', condition:s=&gt;s.lectureCount&gt;=50 },<br>];<br>export async function checkAndAwardBadges(userId: string, supabase: any) {<br>// Gather stats<br>const [profile, quizzes, flashcards, papers, sessions] = await Promise.all([<br>supabase.from('profiles').select('xp,streak').eq('id',userId).single(),<br>supabase.from('quiz_attempts').select('score',{count:'exact'}).eq('user_id',userId),<br>supabase.from('flashcards').select('repetitions').eq('user_id',userId).gt('repetitions',0),<br>supabase.from('past_paper_attempts').select('*',{count:'exact',head:true}).eq('user_id',userId).eq('completed',true),<br>supabase.from('study_sessions').select('started_at').eq('user_id',userId),<br>]);<br>const stats: UserStats = {<br>xp: profile.data?.xp||0,<br>streak: profile.data?.streak||0,<br>quizCount: quizzes.count||0,<br>flashcardCount: (flashcards.data||[]).reduce((s:any,f:any)=&gt;s+f.repetitions,0),<br>paperCount: papers.count||0,<br>lectureCount: 0,<br>perfectQuiz: (quizzes.data||[]).some((q:any)=&gt;q.score&gt;=99),<br>nightStudy: (sessions.data||[]).some((s:any)=&gt;new Date(s.started_at).getHours()&gt;=23),<br>};<br>// Get already-earned badges<br>const { data: earned } = await supabase.from('achievements').select('badge').eq('user_id',userId);<br>const earnedIds = new Set((earned||[]).map((a:any)=&gt;a.badge));<br>// Check and award new badges<br>const newBadges = [];<br>for (const badge of BADGES) {<br>if (!earnedIds.has(badge.id) &amp;&amp; badge.condition(stats)) {<br>await supabase.from('achievements').insert({ user_id:userId, badge:badge.id });<br>newBadges.push(badge);<br>}<br>}<br>return newBadges;<br>}</td></tr></tbody></table>

**4\. STUDY PERSONALITY QUIZ**

**4.1 /app/api/profile/personality/route.ts**
---------------------------------------------

<table><tbody><tr><td>// src/app/api/profile/personality/route.ts<br>import { NextRequest, NextResponse } from 'next/server';<br>import { supabaseServer } from '@/lib/supabase/server';<br>import { requireAuth } from '@/lib/security/adminGuard';<br>import { z } from 'zod';<br>const QUESTIONS = [<br>{ id:1, q:'When studying a new topic, I prefer to...', options:[<br>{a:'See a diagram or chart first', style:'visual'},<br>{a:'Hear someone explain it aloud', style:'auditory'},<br>{a:'Read the textbook definition', style:'reading_writing'},<br>{a:'Try a practice problem immediately', style:'kinaesthetic'},<br>]},<br>{ id:2, q:'I remember information best when...', options:[<br>{a:'I draw it or map it out', style:'visual'},<br>{a:'I say it out loud or discuss it', style:'auditory'},<br>{a:'I write it in my own words', style:'reading_writing'},<br>{a:'I apply it to a real example', style:'kinaesthetic'},<br>]},<br>{ id:3, q:'My lecture notes usually look like...', options:[<br>{a:'Mind maps and coloured diagrams', style:'visual'},<br>{a:'Voice recordings or spoken summaries', style:'auditory'},<br>{a:'Detailed written notes and definitions', style:'reading_writing'},<br>{a:'Worked examples and practice questions', style:'kinaesthetic'},<br>]},<br>{ id:4, q:'When I am stuck on a problem, I...', options:[<br>{a:'Look for a visual explanation online', style:'visual'},<br>{a:'Ask someone to explain it to me', style:'auditory'},<br>{a:'Read more about the concept', style:'reading_writing'},<br>{a:'Try different approaches until one works', style:'kinaesthetic'},<br>]},<br>{ id:5, q:'I find it easiest to understand concepts through...', options:[<br>{a:'Watching demonstrations and videos', style:'visual'},<br>{a:'Group discussions and verbal explanations', style:'auditory'},<br>{a:'Reading articles and textbooks', style:'reading_writing'},<br>{a:'Hands-on practice and experiments', style:'kinaesthetic'},<br>]},<br>];<br>export async function GET() {<br>return NextResponse.json({ questions: QUESTIONS });<br>}<br>export async function POST(req: NextRequest) {<br>const { error, userId } = await requireAuth();<br>if (error) return error;<br>// answers: { questionId: selectedStyle }<br>const { answers } = z.object({ answers:z.record(z.string(),z.string()) }).parse(await req.json());<br>// Count style votes<br>const counts: Record&lt;string,number&gt; = {};<br>Object.values(answers).forEach(s =&gt; { counts[s]=(counts[s]||0)+1; });<br>const winner = Object.entries(counts).sort((a,b)=&gt;b[1]-a[1])[0][0];<br>const supabase = supabaseServer();<br>await supabase.from('profiles').update({ learning_style: winner }).eq('id', userId);<br>return NextResponse.json({ style: winner, counts });<br>}</td></tr></tbody></table>

**5\. WELLBEING FEATURES**

**5.1 Mood Check-in & Burnout Monitor**
---------------------------------------

<table><tbody><tr><td>// src/app/api/wellbeing/mood/route.ts<br>import { NextRequest, NextResponse } from 'next/server';<br>import { supabaseServer } from '@/lib/supabase/server';<br>import { requireAuth } from '@/lib/security/adminGuard';<br>import { z } from 'zod';<br>export async function POST(req: NextRequest) {<br>const { error, userId } = await requireAuth();<br>if (error) return error;<br>const { mood, sleepHours } = z.object({<br>mood: z.number().int().min(1).max(5),<br>sleepHours: z.number().min(0).max(24).optional(),<br>}).parse(await req.json());<br>const supabase = supabaseServer();<br>const today = new Date().toISOString().split('T')[0];<br>await supabase.from('mood_checkins').upsert({<br>user_id: userId, mood, sleep_hours: sleepHours, noted_at: today,<br>}, { onConflict:'user_id,noted_at' });<br>// Burnout check: mood &lt;= 2 for 3+ consecutive days<br>const { data: recent } = await supabase.from('mood_checkins')<br>.select('mood').eq('user_id',userId!).order('noted_at',{ascending:false}).limit(3);<br>const burnoutRisk = (recent||[]).length &gt;= 3 &amp;&amp; (recent||[]).every(r=&gt;r.mood&lt;=2);<br>return NextResponse.json({ ok:true, burnoutRisk });<br>}<br>export async function GET(req: NextRequest) {<br>const { error, userId } = await requireAuth();<br>if (error) return error;<br>const supabase = supabaseServer();<br>const { data } = await supabase.from('mood_checkins')<br>.select('mood, sleep_hours, noted_at')<br>.eq('user_id',userId!).order('noted_at',{ascending:false}).limit(14);<br>return NextResponse.json({ checkins: data||[] });<br>}</td></tr></tbody></table>

**5.2 MoodCheckin.tsx Component**
---------------------------------

<table><tbody><tr><td>// src/components/study/MoodCheckin.tsx<br>'use client';<br>import { useState } from 'react';<br>import { motion, AnimatePresence } from 'framer-motion';<br>import { toast } from 'sonner';<br>const MOODS = [<br>{ value:1, emoji:'😫', label:'Exhausted' },<br>{ value:2, emoji:'😔', label:'Low' },<br>{ value:3, emoji:'😐', label:'Okay' },<br>{ value:4, emoji:'😊', label:'Good' },<br>{ value:5, emoji:'🤩', label:'Energised' },<br>];<br>export function MoodCheckin({ onComplete }: { onComplete?:(burnout:boolean)=&gt;void }) {<br>const [selected, setSelected] = useState&lt;number|null&gt;(null);<br>const [done, setDone] = useState(false);<br>async function submit(mood: number) {<br>setSelected(mood);<br>const res = await fetch('/api/wellbeing/mood', {<br>method:'POST', headers:{'Content-Type':'application/json'},<br>body: JSON.stringify({ mood }),<br>});<br>const data = await res.json();<br>setDone(true);<br>if (data.burnoutRisk) {<br>toast('You have been feeling low for a few days. Consider taking a proper break today.', {<br>duration: 8000, icon: '💙',<br>});<br>}<br>onComplete?.(data.burnoutRisk);<br>}<br>if (done) return (<br>&lt;p className='text-sm text-[var(--text-muted)] text-center py-2'&gt;Mood logged. Keep going!&lt;/p&gt;<br>);<br>return (<br>&lt;div className='flex flex-col gap-2'&gt;<br>&lt;p className='text-xs font-medium text-[var(--text-muted)] text-center'&gt;How are you feeling today?&lt;/p&gt;<br>&lt;div className='flex justify-center gap-3'&gt;<br>{MOODS.map(m=&gt;(<br>&lt;motion.button key={m.value} whileHover={{scale:1.2}} whileTap={{scale:0.9}}<br>onClick={()=&gt;submit(m.value)} title={m.label}<br>className='text-2xl p-1.5 rounded-xl hover:bg-[var(--bg-elevated)] transition-colors'<br>&gt;<br>{m.emoji}<br>&lt;/motion.button&gt;<br>))}<br>&lt;/div&gt;<br>&lt;/div&gt;<br>);<br>}</td></tr></tbody></table>

**5.3 Pre-Exam Anxiety Check-in & Break Suggester**
---------------------------------------------------

<table><tbody><tr><td>// src/app/api/wellbeing/anxiety/route.ts<br>import { NextRequest, NextResponse } from 'next/server';<br>import { requireAuth } from '@/lib/security/adminGuard';<br>import { groqChat } from '@/lib/ai/groq';<br>import { z } from 'zod';<br>export async function POST(req: NextRequest) {<br>const { error } = await requireAuth();<br>if (error) return error;<br>const { anxietyLevel, examName } = z.object({<br>anxietyLevel: z.number().int().min(1).max(5), // 1=calm, 5=very anxious<br>examName: z.string().max(100).default('exam'),<br>}).parse(await req.json());<br>const technique = anxietyLevel &gt;= 4 ? 'box breathing (4-4-4-4)' :<br>anxietyLevel === 3 ? '5-4-3-2-1 grounding (name 5 things you see, 4 you hear...)' :<br>'a 2-minute positive visualisation of walking into the exam feeling prepared';<br>const result = await groqChat(<br>'A student has an exam (' + examName + ') and feels anxiety level ' + anxietyLevel + '/5.' +<br>' Recommended technique: ' + technique + '.' +<br>' Write a calm, brief (80 words max) message guiding them through this technique.' +<br>' Be warm, specific, and end with one confidence-building sentence about their preparation.'<br>);<br>return NextResponse.json({ message: result, technique });<br>}<br>// src/app/api/wellbeing/break/route.ts<br>export async function POST(req: NextRequest) {<br>const { error } = await requireAuth();<br>if (error) return error;<br>const { sessionType, durationMins } = z.object({<br>sessionType: z.enum(['slides','flashcards','quiz','calculator','focus']),<br>durationMins: z.number().int().min(1),<br>}).parse(await req.json());<br>const isIntensive = ['quiz','calculator','focus'].includes(sessionType);<br>const breakType = isIntensive<br>? 'a physical break (stand up, stretch arms overhead 3 times, then walk to a window)'<br>: 'a mental reset (close your eyes for 30 seconds, take 3 slow breaths, then look at something far away)';<br>const result = await groqChat(<br>'A student just finished ' + durationMins + ' minutes of ' + sessionType + ' study.' +<br>' Suggest ' + breakType + '.' +<br>' Write 2-3 sentences: the specific activity, why it helps after this type of study, and when to return. Max 60 words.'<br>);<br>return NextResponse.json({ suggestion: result });<br>}</td></tr></tbody></table>

**6\. EFFORT TRACKER**

**6.1 /app/api/wellbeing/effort/route.ts**
------------------------------------------

<table><tbody><tr><td>// src/app/api/wellbeing/effort/route.ts<br>// Calculates effort score — rewards trying, not just succeeding<br>import { NextRequest, NextResponse } from 'next/server';<br>import { supabaseServer } from '@/lib/supabase/server';<br>import { requireAuth } from '@/lib/security/adminGuard';<br>import { startOfWeek, formatISO } from 'date-fns';<br>export async function GET(req: NextRequest) {<br>const { error, userId } = await requireAuth();<br>if (error) return error;<br>const supabase = supabaseServer();<br>const weekStart = formatISO(startOfWeek(new Date(),{weekStartsOn:1}));<br>const [sessions, quizzes, flashcards, papers] = await Promise.all([<br>supabase.from('study_sessions').select('duration').eq('user_id',userId!).gte('started_at',weekStart),<br>supabase.from('quiz_attempts').select('*',{count:'exact',head:true}).eq('user_id',userId!).gte('completed_at',weekStart),<br>supabase.from('flashcards').select('repetitions').eq('user_id',userId!).gt('repetitions',0),<br>supabase.from('past_paper_attempts').select('*',{count:'exact',head:true}).eq('user_id',userId!).gte('started_at',weekStart),<br>]);<br>const studyHours = (sessions.data||[]).reduce((s,r)=&gt;s+(r.duration||0),0)/3600;<br>const effortScore = Math.min(100,<br>(studyHours * 10) +<br>((quizzes.count||0) * 5) +<br>((flashcards.data||[]).length * 2) +<br>((papers.count||0) * 20)<br>);<br>const badge =<br>effortScore &gt;= 80 ? 'High Effort' :<br>effortScore &gt;= 50 ? 'Consistent' :<br>effortScore &gt;= 25 ? 'Improving' : 'Just Starting';<br>return NextResponse.json({ effortScore: Math.round(effortScore), badge, studyHours: parseFloat(studyHours.toFixed(1)) });<br>}</td></tr></tbody></table>

**7\. PROFILE PAGE — COMPLETE PERSONALISATION**

**7.1 /app/api/profile/preferences/route.ts**
---------------------------------------------

<table><tbody><tr><td>// src/app/api/profile/preferences/route.ts<br>import { NextRequest, NextResponse } from 'next/server';<br>import { supabaseServer } from '@/lib/supabase/server';<br>import { requireAuth } from '@/lib/security/adminGuard';<br>import { z } from 'zod';<br>const Schema = z.object({<br>tutor_name: z.string().max(30).optional(),<br>tutor_personality: z.enum(['encouraging','strict','funny','neutral']).optional(),<br>reading_level: z.number().int().min(1).max(5).optional(),<br>tone_preference: z.enum(['academic','casual']).optional(),<br>preferred_language:z.string().max(5).optional(),<br>font_preference: z.enum(['inter','opendyslexic']).optional(),<br>high_contrast: z.boolean().optional(),<br>text_size: z.enum(['sm','md','lg','xl']).optional(),<br>simplified_mode: z.boolean().optional(),<br>low_bandwidth_mode:z.boolean().optional(),<br>theme: z.enum(['light','dark']).optional(),<br>university: z.string().max(100).optional(),<br>degree: z.string().max(100).optional(),<br>year_of_study: z.number().int().min(1).max(8).optional(),<br>study_goal: z.string().max(200).optional(),<br>});<br>export async function PATCH(req: NextRequest) {<br>const { error, userId } = await requireAuth();<br>if (error) return error;<br>const updates = Schema.parse(await req.json());<br>const supabase = supabaseServer();<br>await supabase.from('profiles').update(updates).eq('id', userId!);<br>return NextResponse.json({ ok:true });<br>}</td></tr></tbody></table>

**7.2 Settings Page — Grouped Preferences**
-------------------------------------------

<table><tbody><tr><td>// Settings page structure (implemented as a tabbed form):<br>// Tab 1: Profile<br>// - Full name, university, degree, year of study, study goal<br>// Tab 2: AI Tutor<br>// - Tutor name (text input)<br>// - Tutor personality (4 option cards: Encouraging / Strict / Funny / Neutral)<br>// - Reading level (5-point slider: Simplified → Expert)<br>// - Tone (Academic / Casual toggle)<br>// - Learning style (link to personality quiz)<br>// Tab 3: Display<br>// - Theme (Light / Dark toggle — syncs with next-themes)<br>// - Font (Inter / OpenDyslexic)<br>// - Text size (Small / Normal / Large / Extra Large)<br>// - High contrast toggle<br>// - Simplified mode toggle<br>// - Low bandwidth mode toggle<br>// Tab 4: Language<br>// - Language selector (English, French, Twi, Hausa, Yoruba, Swahili)<br>// - Applied via next-intl locale switch<br>// Tab 5: Notifications<br>// - Toggle per notification type (stored in platform_settings or profiles JSON column)<br>// All changes call PATCH /api/profile/preferences</td></tr></tbody></table>

**8\. BADGE DISPLAY & ACHIEVEMENT TOAST**

**8.1 BadgeToast — Show when badge earned**
-------------------------------------------

<table><tbody><tr><td>// src/components/study/BadgeToast.tsx<br>// Call this after any action that might award a badge<br>'use client';<br>import { toast } from 'sonner';<br>import { BADGES } from '@/lib/badges';<br>export async function checkBadgesAndNotify(userId: string) {<br>const res = await fetch('/api/profile/badges/check', { method:'POST' });<br>const data = await res.json();<br>for (const badgeId of (data.newBadges||[])) {<br>const badge = BADGES.find(b=&gt;b.id===badgeId);<br>if (badge) {<br>toast(`${badge.emoji} New Badge: ${badge.name}`, {<br>description: badge.description,<br>duration: 5000,<br>});<br>}<br>}<br>}<br>// src/app/api/profile/badges/check/route.ts<br>import { checkAndAwardBadges } from '@/lib/badges';<br>export async function POST(req: NextRequest) {<br>const { error, userId } = await requireAuth();<br>if (error) return error;<br>const supabase = supabaseServer();<br>const newBadges = await checkAndAwardBadges(userId!, supabase);<br>return NextResponse.json({ newBadges: newBadges.map(b=&gt;b.id) });<br>}</td></tr></tbody></table>

**8.2 BadgeGrid.tsx**
---------------------

<table><tbody><tr><td>// src/components/study/BadgeGrid.tsx<br>import { BADGES } from '@/lib/badges';<br>import { cn } from '@/lib/utils';<br>export function BadgeGrid({ earnedIds }: { earnedIds: string[] }) {<br>const earned = new Set(earnedIds);<br>return (<br>&lt;div className='grid grid-cols-4 sm:grid-cols-6 gap-3'&gt;<br>{BADGES.map(badge =&gt; {<br>const isEarned = earned.has(badge.id);<br>return (<br>&lt;div key={badge.id} title={badge.name + ': ' + badge.description}<br>className={cn('flex flex-col items-center gap-1 p-3 rounded-2xl border text-center cursor-default transition-all',<br>isEarned<br>? 'border-[var(--color-amber-500)] bg-[var(--color-amber-50)] shadow-amber-glow'<br>: 'border-[var(--border-default)] bg-[var(--bg-elevated)] opacity-40 grayscale',<br>)}<br>&gt;<br>&lt;span className='text-2xl'&gt;{badge.emoji}&lt;/span&gt;<br>&lt;p className='text-xs font-semibold text-[var(--text-primary)] leading-tight'&gt;{badge.name}&lt;/p&gt;<br>&lt;/div&gt;<br>);<br>})}<br>&lt;/div&gt;<br>);<br>}</td></tr></tbody></table>

**9\. CHECKLIST**

<table><tbody><tr><td>✓ /lib/xp.ts — level thresholds, getLevel, syncLevel helpers</td></tr></tbody></table>

<table><tbody><tr><td>✓ XPBar.tsx — animated progress bar with gradient from violet to amber</td></tr></tbody></table>

<table><tbody><tr><td>✓ /lib/badges.ts — 15 badge definitions with condition functions, checkAndAwardBadges</td></tr></tbody></table>

<table><tbody><tr><td>✓ /api/profile/badges/check/route.ts — run badge check, return new badge IDs</td></tr></tbody></table>

<table><tbody><tr><td>✓ BadgeToast — calls badge check after actions, shows sonner toast on earn</td></tr></tbody></table>

<table><tbody><tr><td>✓ BadgeGrid.tsx — all badges shown, earned ones glow with amber shadow</td></tr></tbody></table>

<table><tbody><tr><td>✓ /api/profile/personality/route.ts — 5 VARK questions, save style to profile</td></tr></tbody></table>

<table><tbody><tr><td>✓ /api/wellbeing/mood/route.ts — daily check-in, burnout detection after 3 low days</td></tr></tbody></table>

<table><tbody><tr><td>✓ MoodCheckin.tsx — 5 emoji buttons, burnout toast if triggered</td></tr></tbody></table>

<table><tbody><tr><td>✓ /api/wellbeing/anxiety/route.ts — Groq generates calming technique for exam anxiety</td></tr></tbody></table>

<table><tbody><tr><td>✓ /api/wellbeing/break/route.ts — Groq suggests break activity after study session</td></tr></tbody></table>

<table><tbody><tr><td>✓ /api/wellbeing/effort/route.ts — effort score rewards attempts not just scores</td></tr></tbody></table>

<table><tbody><tr><td>✓ /api/profile/preferences/route.ts — update all personalisation settings</td></tr></tbody></table>

<table><tbody><tr><td>✓ Settings page — 5 tabs: Profile, AI Tutor, Display, Language, Notifications</td></tr></tbody></table>

<table><tbody><tr><td><strong>Next Document</strong></td><td>Doc 13 — Analytics &amp; Insights</td></tr></tbody></table>

<table><tbody><tr><td><strong>Doc 13 Adds</strong></td><td>Study heatmap, learning velocity, blind spot detector, best study time, benchmarking, quiz history, grade charts, session analytics</td></tr></tbody></table>

UniStudy AI | Document 12 of 18 | Gamification, Wellbeing & Personalisation | Continues from Doc 11